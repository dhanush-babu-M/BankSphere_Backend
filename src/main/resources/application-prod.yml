spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://gateway01.ap-southeast-1.prod.aws.tidbcloud.com:4000/test?sslMode=VERIFY_IDENTITY}
    username: ${DB_USERNAME:2oYjpasWQsfwgdk.root}
    password: ${DB_PASSWORD:lNTOiBM7C7ACH8UW}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

server:
  ssl:
    enabled: false

logging:
  level:
    com.banksphere: WARN
    root: ERROR
